version: '3.8'

services:
  backend:
    build:
      context: ./shop-backend
      dockerfile: Dockerfile
    ports:
      - "5002:5002"
    environment:
      - PORT=5002
      - NODE_ENV=production
